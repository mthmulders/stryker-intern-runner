const { describe, it } = intern.getPlugin('interface.bdd');

describe('Add', function() {
    it('should be able to add two numbers', function() {
        this.skip('don\'t expect this one to run');
        // expect(actual).to.eq(expected);
    });
});
